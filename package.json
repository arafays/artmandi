{
    "name": "artmandi",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "start": "if-env NODE_ENV=production && yarn run -s serve || yarn run -s dev",
        "build": "preact build",
        "serve": "preact build && preact serve --server config",
        "dev": "preact watch",
        "test": "eslint src && preact test"
    },
    "eslintConfig": {
        "extends": "eslint-config-synacor"
    },
    "devDependencies": {
        "eslint": "^4.10.0",
        "eslint-config-synacor": "^2.0.2",
        "firebase": "^4.6.1",
        "if-env": "^1.0.0",
        "node-sass": "^4.5.3",
        "preact-cli": "^2.0.2",
        "sass-loader": "^6.0.6"
    },
    "dependencies": {
        "preact": "^8.2.6",
        "preact-compat": "^3.17.0",
        "preact-redux": "^2.0.3",
        "preact-router": "^2.5.7",
        "redux": "^3.7.2",
        "redux-thunk": "^2.2.0",
        "window-or-global": "^1.0.1"
    }
}
