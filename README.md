### Commands

`preact create art-mandi`: will not need again

`firebase init` to init firebase project


**Important for deploy**
```sh
yarn serve -- --server config
```
***copy generated config from cli and paste it in ***

`/firebase.json`

### Deploy
```sh
firebase deploy
```